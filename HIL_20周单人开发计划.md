# HIL硬件在环仿真飞行验证系统20周单人开发计划

> **实施原则：** 以投标文件的HIL系统要求为范围边界；Z字航线仅作为UE4联调回归用例，不能成为真实HIL的唯一控制方式或验收依据。

**目标：** 在20周内交付一套可在Ubuntu 18.04 RT目标机上运行的HIL最小可验收系统：支持模型工程与实时解算、真实物理I/O闭环、任务与指控、数据记录回放、视频注入、UE显示联动及验收证据输出。

**架构：** 将现有“Simulink/ERT模型 → 1ms C实时核 → Python协议桥 → UE4”的垂直切片扩展为“B/S管控 → 实时核 → 真实I/O/被测件 → 数据与视频 → UE显示”的闭环。实时核只负责确定性计算和设备收发；B/S、存储、报表、视频编排运行在非实时服务侧。

**技术基线：** Ubuntu 18.04 RT、MATLAB R2018b + Simulink/Embedded Coder、GCC 7.x、Python 3.6.9、C11、Vue3 + Element Plus、Spring Boot 2.7.x、达梦DM V8.4、CANFD/车载以太网/反射内存卡厂商SDK、GStreamer、ROS 2、PX4 SITL、Gazebo、UE4接口V2.0。

## 全局约束与验收红线

- 实时模型固定步长为1ms；真实HIL模式必须启用`SCHED_FIFO`和`mlockall`，不允许以普通Windows运行结果代替目标机结果。
- 同一时刻只允许一个控制源写入`flight_control`：`demo_mission`、`px4_sitl`、`physical_uut`三选一；真实HIL模式默认使用`physical_uut`。
- Z字航线只能在`ue4_demo`模式自动装载；进入`hil`模式时必须显式禁用内部任务控制器，避免覆盖被测飞控输出。
- UE4只接收经过协议投影的50Hz飞参/任务数据；不承担动力学解算、硬件控制或安全决策。
- 模型、ICD、场景、任务、视频和验收数据必须带版本、哈希、时间戳和操作者信息。
- 生产运行只启动已验证的模型包；禁止在生产运行入口临时构建模型或切换未签名/未校验的二进制。

## 当前起点

已具备：模型合约校验、MATLAB/ERT生成链路、1ms C实时循环、任务控制器、UDP状态、Python TCP协议桥、UE4 V2协议自检、历史Ubuntu验收证据。

未具备：真实CANFD/车载以太网/反射内存驱动、被测件闭环、PX4/Gazebo/ROS联合仿真、B/S控制台、数据库数据闭环、视频注入及同步、真实UE4目标机验收、当前HEAD目标机验收证据。

## 模块与职责

| 模块 | 主要职责 | 当前代码/新工程位置 |
|---|---|---|
| 实时核 | 1ms调度、模型步进、控制源仲裁、I/O时序、安全停机 | `c_core/src/` |
| 模型工具链 | Simulink模型、ERT生成、模型合同和模型包校验 | `matlab_scripts/`、`models/` |
| 硬件适配层 | 反射内存、CANFD、车载以太网、串口、离散量/模拟量驱动 | 新建`c_core/src/hal/` |
| HIL服务 | 状态采集、协议桥、数据归档、视频编排、REST接口 | `python_services/`与新建`hil-service/` |
| B/S控制台 | 模型工程、试验控制、曲线、任务、回放、告警 | 新建`hil-ui/` |
| UE4联动 | 任务、飞参、显示状态的协议适配和真实会话验证 | `python_services/bridge_tcp_client.py` |
| 验收与运维 | 环境检查、性能证据、部署、日志、报告和手册 | `scripts/`、`deploy/`、`docs/` |

## 每周开发计划

| 周次 | 开发目标与主要工作 | 当周交付物 | 验收标准 |
|---|---|---|---|
| 第1周 | **恢复工程基线。** 修复当前脚本/运行手册测试与自动构建入口不一致的问题；在目标Ubuntu机固定Python依赖、MATLAB、GCC和RT内核版本；将所有验收产物写入带Git提交号的目录。 | 当前HEAD的测试报告；环境指纹；基线验收包；问题清单。 | Python单元测试全绿；MATLAB ERT构建成功；`result.json`记录当前Git SHA；不得使用7月27日旧证据替代当前版本证据。 |
| 第2周 | **冻结HIL边界和ICD。** 明确被测件、反射内存卡、CANFD/车载以太网、串口、模拟量/离散量板卡及驱动SDK；定义控制源、信号方向、单位、频率、超时、复位和故障策略。 | 《HIL接口控制文件》v1；通道映射表；硬件接线图；控制源状态机。 | 每个信号有唯一ID、方向、物理单位、采样周期和失效行为；`demo_mission/px4_sitl/physical_uut`互斥规则通过评审。 |
| 第3周 | **重构硬件适配层。** 将`hal_stub.c`拆为统一HAL接口、仿真后端和真实后端；实现设备发现、初始化、关闭、错误码和健康状态抽象。 | `hal/hal_api.h`、`hal_sim.c`、硬件后端骨架、HAL单元测试。 | 在无硬件条件下仿真后端可跑通；真实后端在缺驱动/缺设备时明确失败且不启动实时循环。 |
| 第4周 | **反射内存链路。** 接入厂商SDK，完成共享区布局、版本头、CRC/序号、双缓冲、读写超时和双通道健康检测。 | 反射内存驱动模块；共享内存协议；回环测试程序。 | 100Mbps以上传输；单帧数据校验、超时、重复帧和错序帧均有可观测告警；连续1小时回环无错帧。 |
| 第5周 | **总线与物理I/O。** 接入CANFD、车载以太网和必要的串口/IO；实现收发队列、硬件时间戳、ICD解析入口和总线断线检测。 | I/O驱动；CANFD/以太网回环用例；信号映射配置。 | 可从真实接口收发至少一组受控测试报文；时间戳完整；断线、CRC失败和超时能在状态页/日志中定位。 |
| 第6周 | **实时性与安全模式。** 统一1ms任务、I/O收发、模型步进和状态发布的时序；增加安全停机、看门狗、过载降级、控制权切换与数据冻结。 | 实时性能采集模块；安全状态机；控制源仲裁测试。 | 10分钟空载和I/O负载测试均输出P99/最大迟到；`physical_uut`模式下内部Z任务无法写入控制量；复位/急停后输出进入安全值。 |
| 第7周 | **模型输入真正闭环。** 将环境、故障、物理I/O和任务/控制信号绑定至Simulink根输入；移除“任务只用于UE4展示”的限制。 | 新版`hil_contract.json`；模型输入映射；模型回归用例。 | 修改风、传感器偏置、丢包率和控制输入后，模型输出在规定周期内出现可预测变化；未声明输入一律拒绝。 |
| 第8周 | **真实HIL模型与被测件联调。** 建立被测飞控或等效硬件的输入输出闭环；完成起飞、悬停、航迹、降落四类基础工况。 | 被测件接线确认单；闭环日志；基础飞行试验记录。 | 四类工况可重复执行；被测件指令、模型状态和I/O报文具备同一时间基准；异常时系统不继续输出危险控制量。 |
| 第9周 | **PX4/Gazebo/ROS联合仿真。** 将PX4 SITL、Gazebo和ROS 2作为可选控制源/场景源接入，不替代真实HIL链路。 | ROS消息映射；PX4/Gazebo启动配置；联合仿真脚本。 | 支持至少两种飞行模式和一条航线；仿真时钟与模型时间误差可测；可切换到真实HIL模式且不会残留控制权。 |
| 第10周 | **B/S服务与最小控制台。** 建立Spring Boot服务、达梦数据模型和Vue3页面骨架；完成登录、角色、试验启动/暂停/复位、设备状态和实时飞参曲线。 | `hil-service/`、`hil-ui/`、数据库迁移脚本、API文档。 | 浏览器可完成一次试验生命周期；权限隔离管理员/操作员/查看者；服务重启后试验记录与操作日志可追溯。 |
| 第11周 | **任务与航线规划。** 将任务、航点、目标、场景参数通过B/S页面配置并下发；Z字任务保留为只读回归模板。 | 任务管理页面；航线编辑与预览；任务版本表。 | 支持多点、圆形、多边形或自由曲线中的至少三类航线；`ue4_demo`可加载Z模板，`hil`模式必须由操作员选择并确认任务。 |
| 第12周 | **数据记录、回放与报告。** 归档原始I/O、飞参、控制命令、事件和配置快照；支持按试验检索、变速回放和基础报告。 | 时序数据存储；回放服务；试验报告模板。 | 可复现一条完整试验的飞参与事件顺序；导出CSV/PDF；报告包含模型、ICD、任务、操作者和版本哈希。 |
| 第13周 | **视频资源与标注。** 支持H.264/H.265/RAW视频加载、资源校验、时间轴标注、飞行阶段/事件触发规则和数据集元信息。 | 视频资源库；标注格式；视频管理页面。 | 视频、标注、任务和模型可关联查询；错误格式、损坏文件和缺失标注被拒绝；支持至少起飞/巡航/目标出现三种触发条件。 |
| 第14周 | **视频注入与时钟同步。** 使用GStreamer实现手动、周期、外部事件和仿真时钟触发；实现SEI时间戳或等效帧级元数据同步。 | 图像注入服务；同步协议；注入日志与测试工具。 | 视频帧、模型时间和飞行状态可按帧关联；暂停、恢复、跳时和丢帧有确定行为；30分钟注入无资源泄漏。 |
| 第15周 | **真实UE4联调。** 使用真实UE4端替代Mini-UE4；验证握手、任务、50Hz飞参、坐标转换、状态超时、断线重连和显示一致性。 | UE4真实联调记录；协议抓包；场景演示脚本。 | `hello`和`mission_plan`均获匹配ACK；状态流频率49–51Hz；位置、姿态和航迹与实时核记录一致；UE断连不得影响HIL安全运行。 |
| 第16周 | **故障注入与跨系统闭环。** 联动总线测试、HIL、视频注入和UE显示，覆盖通信延迟、丢包、传感器偏置、电机失效、总线异常与恢复。 | 故障用例集；告警规则；故障试验报告。 | 每个故障都有触发、检测、告警、处置、恢复和证据；故障不允许造成控制源混乱或非预期继续输出。 |
| 第17周 | **系统功能验收。** 将投标功能逐项映射为可执行测试：模型工程、实时解算、物理接入、参数显示、回放、任务/场景、视频注入、地面站模拟。 | 《HIL功能验收大纲》；需求-测试追踪矩阵。 | 每条投标HIL要求至少对应一个测试用例、日志位置和判定标准；不存在“界面展示但无真实数据来源”的验收项。 |
| 第18周 | **性能与稳定性验收。** 开展1ms实时、I/O负载、50Hz显示、视频注入和连续运行测试；修复确定性问题。 | 性能报告；24小时稳定性记录；问题闭环单。 | 连续24小时无崩溃、无控制源冲突；实时迟到、数据丢失、视频丢帧和总线错误均有统计与阈值结论。 |
| 第19周 | **交付演示与运维。** 形成演示场景：真实被测件/等效硬件、飞行任务、故障注入、视频注入、UE展示、数据回放和报告导出。 | 演示脚本；部署包；操作手册；故障处置手册。 | 独立按手册完成部署、启动、演示、停止和日志导出；不得依赖开发机临时配置或手工改源码。 |
| 第20周 | **终验收收口。** 修复遗留缺陷，冻结版本和模型包，整理源代码、安装包、ICD、测试报告、培训材料和终验收证据。 | 最终验收包；版本清单；培训签到和问题清单。 | 所有P0/P1缺陷关闭；完整验收演示通过；每份交付物可通过哈希、版本和配置追溯至测试结果。 |

## 每周固定节奏

- 周一：确定本周唯一可验收目标与风险；冻结新增需求，避免单人开发并行分散。
- 周二至周四：按“先失败测试、再最小实现、再目标机验证”推进；每天保留可回退提交。
- 周五：生成当周证据包，更新需求-测试追踪矩阵和风险台账；未达到验收标准的工作不得标记完成。

## 关键依赖与止损规则

| 风险 | 最晚决策点 | 止损规则 |
|---|---|---|
| 硬件板卡型号/SDK未到位 | 第2周结束 | 第3周仅完成HAL仿真后端；第4周起需调整计划并将真实HIL验收风险上报。 |
| Ubuntu RT或MATLAB R2018b环境不可用 | 第1周结束 | 停止新增功能，优先解决目标环境；Windows结果不得用于宣称实时HIL完成。 |
| Vue/Spring从零搭建影响实时主线 | 第10周 | 只实现HIL必需页面；通用账号、审批、文件管理复用综合管理系统，不重复建设。 |
| UE4端不可获得匹配协议ACK | 第15周开始前 | 维持Mini-UE4作回归工具，但真实UE4验收项标记未通过，不能以本地模拟替代。 |
| 视频源或SEI方案不确定 | 第13周结束 | 使用统一帧级时间戳元数据先完成同步验证；SEI封装在第14周按目标端能力落地。 |

## 最终验收最小场景

1. 操作员在B/S端选择已验证模型、ICD和任务，系统完成权限校验与版本记录。
2. `hil`模式接入真实被测件或等效硬件；内部Z任务控制器被禁止写入控制量。
3. 模型以1ms步长运行，CANFD/车载以太网/反射内存完成闭环数据收发。
4. 触发一项总线或传感器故障，系统记录、告警并按安全策略处置。
5. 注入带时间戳的视频流；UE4实时显示飞行姿态、航迹和任务/目标状态。
6. 停止试验后，从B/S端回放数据并导出包含模型、任务、ICD、操作员、日志和结论的报告。

## 计划自检

- 投标文件要求的模型工程、实时解算、物理接口、Simulink接口、指控、数据回放、视频注入、场景/目标规划、B/S管理与验收均已落到明确周次。
- Z字航线被限制为`ue4_demo`回归测试，不再覆盖真实被测件控制输入。
- 真实硬件、真实UE4、当前HEAD目标机证据被列为独立验收门，避免用静态测试或旧证据替代。
