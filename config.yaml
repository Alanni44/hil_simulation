# HIL Simulation Config
# Restart Python services after changes, no recompile needed

local_udp:
  command_port: 9997
  status_port: 9998

spring_boot:
  websocket_port: 8100

ue4_tcp:
  port: 8889
  send_hz: 10

logging:
  level: "INFO"
  file: "logs/hil.log"
